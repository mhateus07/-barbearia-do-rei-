# Produção (SaaS multi-tenant): front e API vivem no mesmo subdomínio por
# tenant, então a baseURL relativa padrão (/api/v1) já funciona sem precisar
# definir VITE_API_URL aqui. Só defina se a API rodar em um domínio diferente.
