# Opcional: em dev local o front (porta 5173) e a API (porta 3333) rodam em
# origens diferentes, então é preciso apontar explicitamente para a API.
# Em produção isso não é necessário (front e API ficam no mesmo subdomínio).
VITE_API_URL=http://localhost:3333/api/v1
